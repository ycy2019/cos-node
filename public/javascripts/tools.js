let path = require("path")
module.exports = {
    checkFileType: function (filename) {
        let basename = path.extname(filename).toLowerCase().slice(1, basename.length);
        if (basename)
    }
}